version: '3.4'

services:
   db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: 123456
      POSTGRES_DB: "gobarberts"
    ports:
      - "5432:5432"

